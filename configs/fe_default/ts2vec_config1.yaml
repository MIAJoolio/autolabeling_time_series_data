model_init:
  output_dims: 64
  hidden_dims: 64
  depth: 10
  lr: 0.001
  device: "cuda:1"
  batch_size: 8
model_training:
  n_epochs: 20
  # n_iters: 200
  verbose: True
encode_params:
  encoding_window: True # значит используется 'full_series'
  sliding_length: 1
  sliding_padding: 50
  batch_size: 1