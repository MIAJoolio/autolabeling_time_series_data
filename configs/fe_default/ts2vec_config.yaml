model_params:
  output_dims: 320
  hidden_dims: 64
  depth: 10
  n_epochs: 10
  lr: 0.001
encode_params:
  encoding_window: True # значит используется 'full_series'
  sliding_length: 1
  sliding_padding: 50